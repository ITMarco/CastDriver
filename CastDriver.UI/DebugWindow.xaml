<Window x:Class="CastDriver.UI.DebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CastDriver — Debug"
        Icon="icon.ico"
        Width="640" Height="520"
        WindowStartupLocation="CenterScreen"
        Background="#1C1C1E">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Dark.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Diagnostics"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <!-- Controls -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <CheckBox x:Name="LoggingCheck"
                          Content="Enable logging"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          FontSize="13"
                          Checked="OnToggleLogging" Unchecked="OnToggleLogging"/>
                <TextBlock x:Name="PathText"
                           Style="{StaticResource SmallStyle}"
                           Margin="0,6,0,0" TextWrapping="Wrap"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Refresh" Click="OnRefresh"
                        Style="{StaticResource GhostButtonStyle}"
                        Padding="12,6" FontSize="12" Margin="0,0,8,0"/>
                <Button Content="Delete log" Click="OnDelete"
                        Style="{StaticResource GhostButtonStyle}"
                        Padding="12,6" FontSize="12"/>
            </StackPanel>
        </Grid>

        <!-- Log contents -->
        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                CornerRadius="8">
            <TextBox x:Name="LogText"
                     IsReadOnly="True"
                     Background="Transparent"
                     Foreground="{StaticResource TextSecondaryBrush}"
                     BorderThickness="0"
                     FontFamily="Consolas" FontSize="12"
                     Padding="10"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"/>
        </Border>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Close" Click="OnClose"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Padding="16,6" FontSize="12"/>
        </StackPanel>
    </Grid>
</Window>
